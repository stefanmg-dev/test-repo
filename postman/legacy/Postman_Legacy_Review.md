# Legacy Postman review

Keep the previous collection as `Legacy Manual Tests`.

Keep manually confirmed real-document and configuration-lifecycle requests. Move temporary, duplicate, blank-body, and generated OpenAPI example requests into clearly named legacy folders. Delete `__MACOSX`, `._*`, empty URLs, generated placeholder examples, and obsolete duplicates only after the stable collection is verified. Never commit real invoices, raw responses, local absolute paths, credentials, or personal data.
